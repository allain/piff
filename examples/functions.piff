// simple function
fn add(a, b) {
  return a + b
}

fn div(n, d=1) {
  return n / d
}

// anonymous function
multiply = fn(a, b) {
  return a * b
}

// anonymous function using external variables
ticks = []
tick = fn() {
  ticks[] = mktime()
}