{
  "name": "piff",
  "version": "0.1.0",
  "main": "index.js",
  "license": "ISC",
  "bin": {
    "piff": "./bin/piff"
  },
  "watch": {
    "test": "{src,test}/*.js"
  },
  "scripts": {
    "build": "pegjs -o piff-parser.js piff.pegjs",
    "test": "tape test/**/*.test.js",
    "watch": "npm-watch"
  },
  "dependencies": {
    "minimist": "^1.2.0"
  },
  "devDependencies": {
    "npm-watch": "^0.1.9",
    "pegjs": "^0.10.0",
    "tape": "^4.6.3"
  }
}
