{
  "name": "piff",
  "version": "0.7.2",
  "main": "index.js",
  "license": "ISC",
  "bin": {
    "piff": "./bin/piff.js"
  },
  "scripts": {
    "build": "pegjs -o piff-parser.js piff.pegjs",
    "test": "npm run build && jest --runInBand",
    "test:noisy": "pegjs -o piff-parser.js --trace piff.pegjs && jest --runInBand",
    "test:coverage": "npm run build && jest --runInBand --coverage"
  },
  "dependencies": {
    "flatten": "^1.0.2",
    "lazy.js": "^0.5.0"
  },
  "devDependencies": {
    "@types/lazy.js": "^0.3.28",
    "jest": "^20.0.4",
    "eslint": "^3.19.0",
    "fs-extra": "^3.0.1",
    "pegjs": "^0.10.0"
  },
  "jest": {
    "verbose": true,
    "testEnvironment": "node"
  }
}